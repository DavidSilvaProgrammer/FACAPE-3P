/*• Dados a lista de convidados de uma festa e o nome de uma pessoa, determinar se essa pessoa é ou não convidada da festa. Codifique um programa completo para resolver esse problema. Crie um procedimento para fazer a entrada da lista de convidados e adapte a função buscaSeq para verificar se o nome consta ou não da lista.*/


#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int tamanho,l,i;
    char nomes[tamanho][20],procurado[20];
    
    int buscaSeq(char *v, int tamanho, char procurado);
    
    printf("Digite o numero de convidados da festa: ");
    scanf("%d",&tamanho);

    printf("Tamanho: %d\n", tamanho);
    
    for(l=1;l<tamanho;l++){
        printf("Digite o nome do convidado da posicao %d: ",l+1);
        setbuf(stdin, 0);
        fgets(nomes[l], 20, stdin);
        
    }
    printf("*******\n");
    for (l=0;l<5;l++){
        printf("Nome na posicao %d: %s", l+1, nomes[l]);
    }
    printf("*******\n");
    printf("Digite o nome da pessoa que deseja procurar na lista de convidados da festa:\n");
    setbuf(stdin,0);
    fgets(procurado,20,stdin);
    
    printf("Nome do procurado: %s", procurado);
    
if (buscaSeq(nomes[20],tamanho, procurado[20]) == 5000){
    printf("O nome procurado nao se encontra na lista");
}else{
    printf("O nome procurado encontra-se na posicao %d da lista",i);
}



    
    return(0);
}

int buscaSeq(char *v, int tamanho, char procurado){
    for (int i=0;i<tamanho;i++){
        if(procurado == v[i]){
            return(i);
        }else{
            return 5000;
        }
    }
}
